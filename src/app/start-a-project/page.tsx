import { ComingSoonPage } from "@/components/ComingSoonPage";

export default function StartAProjectPage() {
  return (
    <ComingSoonPage
      eyebrow="Start a Project"
      title="Project Request"
      description="A dedicated intake page can expand here later. For now, the homepage request form is ready for future integration."
    />
  );
}
